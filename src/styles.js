import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#F5FCFF00"
    },
    borderContainer: {
        borderColor: 'red',
        borderWidth: 1
    },
    textButton: {
        color: "#fff"
    },
    title:{
        fontSize: 24,
        padding: 16,
        fontWeight: "bold"
    }
});